language: cpp
os:
  - osx

compiler:
  - clang

before_script:
  - pip install --user cpp-coveralls

script:
  - mkdir -p build
  - cd build
  - cmake ..
  - cmake --build . -j 4
  - ctest

after_success:
  - coveralls --root .. -E ".*external.*" -E ".*CMakeFiles.*" -E ".*test/.*.cpp.*"
